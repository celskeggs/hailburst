Import('env')

env["CPPPATH"] += [
    Dir("#/linux/include/"),
]

sources = [
    "fakewire_link.c",
    "platform.c",
    "semaphore.c",
]

objects = [env.Object(source) for source in sources]

objects += [
    "data.ld",
]

Return('objects')
